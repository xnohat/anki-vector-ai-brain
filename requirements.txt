# Vector Brain Agent — dependencies

# Core
openai>=1.40.0          # brain (gpt-5.5), STT, TTS, embeddings
numpy>=1.21.0           # memory recall, sensor math
Pillow>=9.0.0           # camera frames -> vision
opencv-python>=4.5.0    # camera frame handling

# Vector SDK (install separately: pip install -e vector-python-sdk/)
protobuf<=3.20.3

# Optional: OFFLINE speech-to-text fallback (VECTOR_STT_BACKEND=local).
# The default is fast cloud STT, so these are NOT required.
# openai-whisper>=20231117
# torch>=2.0.0
